server:
  port: 8001
mybatis:
  config-location: classpath:mybatis/mybatis.cfg.xml        # mybatis配置文件�?在路�?
  type-aliases-package: com.hengyangshiyuan.hrsystem.bean    # �?有Entity别名类所在包
  mapper-locations:
  - classpath:mybatis/mapper/**/*.xml                       # mapper映射文件
    
spring:
   application:
    name: hrsystem-manager
   datasource:
    type: com.alibaba.druid.pool.DruidDataSource            # 当前数据源操作类�?
    driver-class-name: org.gjt.mm.mysql.Driver              # mysql驱动�?
    url: jdbc:mysql://localhost:3306/cloudDB01              # 数据库名�?
    username: root
    password: 345177
    dbcp2:
      min-idle: 5                                           # 数据库连接池的最小维持连接数
      initial-size: 5                                       # 初始化连接数
      max-total: 5                                          # �?大连接数
      max-wait-millis: 200                                  # 等待连接获取的最大超时时�?